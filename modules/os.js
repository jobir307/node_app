const os = require("node:os")

// platform
// console.log(os.platform());

// information
// console.log(os.cpus());

// architecture
// console.log(os.arch());

// total memory in disk
// console.log(os.freemem());

// free memory in disk 
// console.log(os.totalmem());

// entry point
console.log(os.homedir());
